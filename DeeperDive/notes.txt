LESSON 1 - 


package main

import "fmt"

func main() {

	/**
	lets break down the below line -
	var - to create a new variable
	card - here card is the name of the variable
	string - this bounds that only string values can be assigned to this variable

	Since go is static type we cannot change the value of variables dynamically


	**/
	// var card string = "Ace of cards"
	card := "Ace of cards" //this also does the same thing as above but it tells the go compiler to figure the type of data stored in card and go does that by reading this :=. Go will infer types to a certain degree unlike java or C++

	//Also := is used only when we are defining new values and not during reassigning

	card = "Five of Diamonds"
	fmt.Println(card)
}




///////////////////////////////////////////////////////


LESSON 2 - function infer - we define what the function's return type is going to be for the function

package main

import "fmt"

func main() {

	card := newCard()

	fmt.Println(card)
}

//When we are defining a new function we have to very specific about the type of data we want to return that is why after parenthesis we should declare string to ensure that we are returing string value
func newCard() string {
	return "Five of diamonds"
}




Also if there are two files in the same folder with the same package main , we donot have to import them because in Go, files that belong to the same package can directly access each other's functions without needing to explicitly import them. This means that if multiple files share the same package name, they can collaborate seamlessly. The misconception here may stem from how code organization works in other programming languages, where imports might be necessary for accessing functionality across files. Understanding this Go-specific behavior can help clarify how to structure your project effectively.

///////////////////////////////////////////////////////


LESSON 3 - Arrays and slices 

package main

import "fmt"

func main() {

	card := newCard()

	fmt.Println(card)
}

/**
Go has 2 basic data structures for handling list data structs -

- Arrays - fixed length of things
- Slice - An array that can grow or shrink

Both slices and arrays must have a datatype- meaning we cannot store data of different types in an array or slice

**/

func newCard() string {
	return "Five of diamonds"
}

package main

import "fmt"

func main() {
	//here we are declaring cards as slice
	cards := []string{newCard(), newCard()}
	//Note - append function does not update the original slice , it returns a new slice to which we assign it back to the original
	cards = append(cards, "Six of spades")

	//Iterate of slice of cards and print the result
	/**
	range is a keyword that we use to iterate over the elements in an array or slice

	:= syntax is used because we are redeclaring and reassigning the values of i and card everytime we run the loop
	**/
	for i, card := range cards {
		fmt.Println(i, card)
	}

}

func newCard() string {
	return "Five of diamonds"
}


/////////////////////////////////////////////////////////

LESSON 4 - Go approach vs OOP approach


two files - 

main.go :-------------

package main

func main() {
	cards := deck{"Ace of Diamonds", newCard()}

	cards = append(cards, "Six of spades")

	// for i, card := range cards {
	// 	fmt.Println(i, card)
	// }
	cards.print()

}

func newCard() string {
	return "Five of diamonds"
}






deck.go: -----------

package main

import "fmt"

/**
Create a new type of deck which is a slice of strings which extends the functionality of slice
**/

type deck []string

//here we want to write a new function to print all the values of the cards

//here (d deck) is referred to as the receiver

/**
func (d deck) print(){
}

Any variable of type deck now gets access to the print method.

in the main file we will see that while assigning we have assigned cards to be of type deck. therefore cards will have access to all the functions that are assigned to
**/

func (d deck) print() {
	for i, card := range d {
		fmt.Println(i, card)
	}
}

///////////////////////////////////////////////////////////////

LESSON 5 - we will create a new deck using function




main.go : -------------------------


package main

func main() {
	cards := newDeck()

	cards.print()

}




deck.go : --------------------------

package main

import "fmt"

type deck []string

func newDeck() deck {
	cards := deck{}

	//in order to create the all 52 cobinations we are going to seperate the suits and values in 2 different sets and then use for loop twice to map the values to deck

	cardSuits := []string{"Spades", "Diamonds", "Hearts", "Clubs"}
	cardValues := []string{"Ace", "Two", "Three", "Four"}

	//after this we will setup two for loops nested inside one another to iterate through all the combinations

	//while looping if we have some variable which we are not going to use , we replace that variable with an _
	for _, suit := range cardSuits {
		for _, value := range cardValues {
			cards = append(cards, value+" of "+suit)
		}
	}
	return cards

}

func (d deck) print() {
	for i, card := range d {
		fmt.Println(i, card)
	}
}


///////////////////////////////////////////////////////////////

LESSON 6 - creating deal function - to create a hand of cards and save it on local machine 

main.go
package main

func main() {
	// cards := newDeck()

	// hand, remainingHand := deal(cards, 5)
	// hand.print()
	// remainingHand.print()

	cards := newDeck()
	// fmt.Println(cards.toString())
	cards.saveToFile("my_cards")

}



///////////


deck.go 
package main

import (
	"fmt"
	"os"
	"strings"
)

type deck []string

func newDeck() deck {
	cards := deck{}

	cardSuits := []string{"Spades", "Diamonds", "Hearts", "Clubs"}
	cardValues := []string{"Ace", "Two", "Three", "Four"}

	for _, suit := range cardSuits {
		for _, value := range cardValues {
			cards = append(cards, value+" of "+suit)
		}
	}
	return cards

}

func (d deck) print() {
	for i, card := range d {
		fmt.Println(i, card)
	}
}

//Deck Deal

//in this function the deal function is returning two values - both of type deck

// deal function takes two arguments - deck and handSize which is of int type
func deal(d deck, handSize int) (deck, deck) {
	return d[:handSize], d[handSize:]
}

//Save to file - save a list of cards to a file on a local machine

/**
we are going to use io util package to help with  interacting with  the local machine

We will be using io util writeFile function -
Syntax -
func WriteFile(filename string, data []byte, perm fs.FileMode) error


since writeFile require data to be in byte slice format - we can use typeconversions to convert deck type to string and then byte

SYNTAX - []byte("hi there!")

currently we have deck(which is of type deck) --> []string --> string --> []byte(we want)
We need to follow the above conversion method

its better to write this save to file function inside of a helper file

**/

// this is a receiver function to convert the cards deck type to string
func (d deck) toString() string {
	//since our deck is basically a slice of string , we can condence this slice of string and convert it down to string
	return strings.Join([]string(d), ",")
	//we will use join function which is part of strings package and condence this slice of strings - func Join( a[]string, sep string) string --> return type is string

}

//Now since we have already converted the deck type to string all that is remaining is to save this data on to local machine

func (d deck) saveToFile(filename string) error {
	return os.WriteFile(filename, []byte(d.toString()), 0666)
}



////////////////////////////////////////////////////////


LESSON 7 - create deck from local to main file 


main.go
package main

func main() {
	// cards := newDeck()

	// hand, remainingHand := deal(cards, 5)
	// hand.print()
	// remainingHand.print()

	cards := newDeckFromFile("my")
	cards.print()
	// fmt.Println(cards.toString())
	// cards.saveToFile("my_cards")

}





/////////

deck.go


package main

import (
	"fmt"
	"os"
	"strings"
)

type deck []string

func newDeck() deck {
	cards := deck{}

	cardSuits := []string{"Spades", "Diamonds", "Hearts", "Clubs"}
	cardValues := []string{"Ace", "Two", "Three", "Four"}

	for _, suit := range cardSuits {
		for _, value := range cardValues {
			cards = append(cards, value+" of "+suit)
		}
	}
	return cards

}

func (d deck) print() {
	for i, card := range d {
		fmt.Println(i, card)
	}
}

//Deck Deal

func deal(d deck, handSize int) (deck, deck) {
	return d[:handSize], d[handSize:]
}

//Save to file - save a list of cards to a file on a local machine

func (d deck) toString() string {

	return strings.Join([]string(d), ",")

}

func (d deck) saveToFile(filename string) error {
	return os.WriteFile(filename, []byte(d.toString()), 0666)
}

//load data from local file- just opposite of what we did above
//using readFile function

func newDeckFromFile(filename string) deck {
	bs, err := os.ReadFile(filename)
	if err != nil {
		//option 1 - log the error and return a call to newDeck()

		//option 2 - log the error and exit the program
		fmt.Println("Error:", err)
		os.Exit(1)
	}
	//now we have a bytesize and we want to convert it to slice of string , the exact opposite of what we were trying before with save on local storage
	s := strings.Split(string(bs), ",")
	return deck(s)
}



////////////////////////////////////////////////////////

LESSON 8 - shuffle function - it will randomise the order of the cards

Logic to apply for shuffling since go does not have its own shuffle function

for each index, card in cards
    Generate a random number between 0 and len(cards)-1
    swap the current card and the card at cards[randomNumber]


here we can follow two steps , either use the original source in golang which will keep the seed same so after certain rotations the output would be same or we can create our own source and use it with rand.Intn to generate completely random integers 


//main.go

package main

func main() {
	// cards := newDeck()

	// hand, remainingHand := deal(cards, 5)
	// hand.print()
	// remainingHand.print()

	// cards := newDeckFromFile("my")
	// cards.print()
	// fmt.Println(cards.toString())
	// cards.saveToFile("my_cards")
	cards := newDeck()
	cards.shuffle()
	cards.print()

}



/////////////////////////////////////////////////////////
deck.go 


package main

import (
	"fmt"
	"math/rand"
	"os"
	"strings"
	"time"
)

type deck []string

func newDeck() deck {
	cards := deck{}

	cardSuits := []string{"Spades", "Diamonds", "Hearts", "Clubs"}
	cardValues := []string{"Ace", "Two", "Three", "Four"}

	for _, suit := range cardSuits {
		for _, value := range cardValues {
			cards = append(cards, value+" of "+suit)
		}
	}
	return cards

}

func (d deck) print() {
	for i, card := range d {
		fmt.Println(i, card)
	}
}

//Deck Deal

func deal(d deck, handSize int) (deck, deck) {
	return d[:handSize], d[handSize:]
}

//Save to file - save a list of cards to a file on a local machine

func (d deck) toString() string {

	return strings.Join([]string(d), ",")

}

func (d deck) saveToFile(filename string) error {
	return os.WriteFile(filename, []byte(d.toString()), 0666)
}

//load data from local file- just opposite of what we did above
//using readFile function

func newDeckFromFile(filename string) deck {
	bs, err := os.ReadFile(filename)
	if err != nil {
		//option 1 - log the error and return a call to newDeck()

		//option 2 - log the error and exit the program
		fmt.Println("Error:", err)
		os.Exit(1)
	}
	//now we have a bytesize and we want to convert it to slice of string , the exact opposite of what we were trying before with save on local storage
	s := strings.Split(string(bs), ",")
	return deck(s)
}

//shuffle - to shuffle we will use math/rand function -  func Intn - Intn returns as an int , a non - negative pseudo-random number in [0,n)  from the default Source. It panics if n<=0

// since this is a receiver function we donot need to set any arguments or set any return type since this is only going to shuffle the deck
func (d deck) shuffle() {
	for i := range d {
		//generate random number between 0 and len of deck -1
		//here the source for rand will remain the same so everytime the rand will return the same type of output , so in order to resolve this we create our source and so now everytime the new random series is generated it will be different
		source := rand.NewSource(time.Now().UnixNano())
		r := rand.New(source)
		newPosition := r.Intn(len(d) - 1)
		//swap i with new random position
		d[i], d[newPosition] = d[newPosition], d[i]
	}
}

/////////////////////////////////////////////////////////



LESSON 9 

Creating unit test cases 

- to create unit test cases in golang we need a go.mod file so inside our folder we run this command  :

go mod init folderName (here we have DeeperDive)
Then, you will be able to use the run test function from within VSCode, and/or run go test from the terminal.

Go testing is not like Rspec, mocha , jasmine, selenium exact
to make a test , ceate a new file ending in _test.go
ex : deck_test.go

- to run all tests in a package , run the command 
go test


Errorf : Omitting a formatting directive will now cause some tests to fail so we will need to add these right away

NOTE : this directive was originally added at the end of the lecture 

When running your tests starting around 8:00 timestamp change the following : 
t.Errorf("Expected deck length 16 but got " , len(d))

to this 

t.Errorf("Expected deck length of 16, but got %v", len(d))
